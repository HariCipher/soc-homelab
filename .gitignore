# secrets — never commit
secrets/*
!secrets/*.example.md

# VM artifacts
*.qcow2
*.iso
*.vmdk

# raw logs / captures
*.pcap
*.log
evidence/**/raw/

# noise
.DS_Store
*.swp

# agent tooling state - session scratch, not lab artifacts
.claude/
.claude-flow/
.serena/

# working files - local tracking only, deliberately not published.
# The repo publishes the design, the setup guide, the issues log and the
# detections. How the lab is driven day to day is not portfolio material.
scripts/
STATUS.md
RESUME.md
